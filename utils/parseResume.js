const parseResume = async (resumeUrl) => {
    // Call a third-party API or use mock data
    return { name: 'John Doe', skills: ['JavaScript', 'React'] }; // Mock data
  };
  
  export default parseResume;